#Deployment Pods kubernets
#pod postgres
- name: deployment postgres
  become: yes
  command: cd /home/boomer/Documents | kubectl apply -f ~postgres/
  register: command_result
- debug:
    var: command_result.stdout_lines

#pod app
- name: deployment app
  become: yes
  command: cd /home/boomer/Documents | kubectl apply -f ~deploy-django/
  register: command_result
- debug:
    var: command_result.stdout_lines