pipeline { 
    agent any 
    stages {
        stage('Cloning our Git') { 
            steps { 
                checkout scm
            }
        }
        stage('Building our image') { 
            steps { 
                ansiblePlaybook credentialsId: 'myserver01', inventory: 'ansible/hosts.yml', playbook: 'ansible/Jenkinsfile'                  
                    }
                }
            }
        }
